🛠 Especificaciones Técnicas: Generador de CID (Campaign ID)1. Jerarquía de Selección y Pre-condicionesAntes de iniciar la construcción del string, el sistema debe validar el contexto del usuario:Selector Global (Media Owner): El usuario debe elegir el "Media Owner" (Buentipo, Hermano, LoBueno) antes de listar las Organizaciones/Tenants. Esto filtrará los diccionarios y clientes disponibles.Validación de Rol: Solo los usuarios con permisos de escritura pueden generar nuevos CIDs; los usuarios de consulta solo visualizan el historial.2. Mapeo de Atributos y Origen de DatosEl CID se construirá concatenando los siguientes campos según las reglas de negocio:CampoOrigen de DatoTipo de InputNotas de LógicaMedia OwnerContexto InicialPre-seleccionadoDefine el "dueño" de la data.Marketing ChannelGovernance BuilderHeredadoViene de la tabla Campaign.CountryGovernance BuilderHeredadoViene de la tabla Campaign.Sub-ChannelNuevo CampoDropdownOpciones: PRO, RTG, DYN.PublisherGovernance BuilderHeredadoCampo Clave para la lógica condicional.Campaign NameGovernance BuilderHeredadoNombre descriptivo de la campaña.ObjectiveGovernance BuilderHeredadoEj: VentaFlash, Awareness, etc.Funnel StageGovernance BuilderHeredadoEtapa del embudo.Budget SourceGovernance BuilderHeredadoOrigen de los fondos.Launch DateUsuario (Manual)Date PickerFormato estricto: DD/MM/YYYY.3. Lógica de Tokens Dinámicos (Inyección de IDs)El sistema debe detectar el valor del campo Publisher (heredado del Provider) para inyectar automáticamente los parámetros de seguimiento:[!IMPORTANT]Si Publisher = "Facebook/Meta":Campaign ID = {{campaign.id}}Ad Set ID = {{adset.id}}Ad ID = {{ad.id}}Si Publisher = "Google Ads":Campaign ID = {campaignid}Ad Set ID = {adgroupid}Ad ID = {creative}_{matchtype}_{keyword}4. Generación del String Final (Output)El sistema debe concatenar los valores utilizando un separador estándar (ej. guion bajo _ o pipe | según se defina en la configuración del Media Owner).Ejemplo de salida para Google Ads:COL_PRO_Google_BlackFriday_VentaFlash_LowerFunnel_ClientBudget_15112024_Buentipo_{campaignid}_{adgroupid}_{creative}5. Reglas de PersistenciaID Aleatorio Unico: Además de los tokens de plataforma, el sistema debe asignar un UID interno de base de datos para cada CID generado.Inmutabilidad: Una vez que el Trafficker guarda el CID en el historial del cliente, los campos heredados no pueden editarse para ese registro específico.